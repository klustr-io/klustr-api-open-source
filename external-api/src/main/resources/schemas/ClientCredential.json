{
  "title": "ClientCredential",
  "properties": {
    "access_token_strategy": {
      "type": "string"
    },
    "subject": {
      "description": "The subject that gave this consent",
      "type": "string"
    },
    "total_interaction_count": {
      "description": "The estimated number of logins for the user and consenting to the app.",
      "type": "integer"
    },
    "date_first_interaction": {
      "description": "The date the user first interacted with consent for this app.",
      "type": "string",
      "format": "date-time"
    },
    "date_last_interaction": {
      "description": "The date the user last interacted with consent for this app.",
      "type": "string",
      "format": "date-time"
    },
    "approved_scopes": {
      "description": "The scopes that were approved by the user.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "experiments": {
      "type": "array",
      "description": "The experiments this user has opted-into.",
      "items": {
        "type": "object",
        "title": "UserConsentExperiment",
        "description": "Record of a users consent to an experiment",
        "properties": {
          "id": {
            "type": "string",
            "description": "THe ID of the experiment that was consented to."
          },
          "first_seen_date": {
            "description": "The first consent date this experiment was last consented to.",
            "type": "string",
            "format": "date-time"
          },
          "last_seen_date": {
            "description": "The last consent date this experiment was last consented to.",
            "type": "string",
            "format": "date-time"
          },
          "metadata": {
            "type": "object",
            "title": "UserConsentExperimentReference",
            "description": "The metadata for display",
            "properties": {
              "name": {
                "type": "string",
                "description": "THe name of this experiment."
              },
              "description": {
                "type": "string",
                "description": "The description of this experiment."
              },
              "scopes": {
                "description": "The scopes that are associated to this experiment.",
                "type": "array",
                "items": {
                  "type": "string"
                }
              }
            }
          }
        }
      }
    }
  },
  "required": [
    "id"
  ]
}