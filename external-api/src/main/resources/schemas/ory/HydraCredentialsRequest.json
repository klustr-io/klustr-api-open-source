{
  "title": "HydraCredentialsRequest",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "properties": {
    "access_token_strategy": {
      "type": "string",
      "description": "The access token type generated for this credential request. Defaults to 'OPAQUE'.",
      "default": "OPAQUE",
      "existingJavaType": "io.klustr.schemas.console.AccessTokenStrategy"
    },
    "token_endpoint_auth_method": {
      "type": "string",
      "description": "The authentication method to use when issuing the request. Defaults to 'CLIENT_SECRET_BASIC'",
      "default": "CLIENT_SECRET_BASIC",
      "existingJavaType": "io.klustr.schemas.console.TokenEndpointAuthMethod"
    },
    "name": {
      "type": "string",
      "description": "The display name for this client credential"
    },
    "audience": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "existingJavaType": "java.util.Set<String>",
      "examples": [
        "spiffe://staging.example.com/payments/web-fe",
        "spiffe://example.com/9eebccd2-12bf-40a6-b262-65fe0487d453",
        "my_service_xyz"
      ]
    },
    "client_id": {
      "type": "string",
      "description": "The optional client_id to specify which must be a unique UUID v4 format, if not specified will be generated automatically."
    },
    "client_secret": {
      "type": "string",
      "description": "The optional secret to specify when creating your credential, if not specified will be generated automatically."
    },
    "status": {
      "type": "string",
      "description": "The status of this OIDC credential which can be disabled or otherwise paused for security reasons.",
      "default": "ENABLED",
      "existingJavaType": "io.klustr.schemas.console.OidcStatus"
    },
    "scopes": {
      "description": "The scopes to apply and update for the client.",
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },
  "required": [
    "name"
  ]
}