{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "LoadBalancerBackendResource",
  "description": "A backend managed by the loadbalancer",
  "type": "object",
  "properties": {
    "mode": {
      "type": "string",
      "description": "The mode for the load balancer",
      "enum": [
        "http", "tcp"
      ]
    },
    "name": {
      "type": "string",
      "description": "The unique name for this backend resource."
    }
  },
  "required": []
}