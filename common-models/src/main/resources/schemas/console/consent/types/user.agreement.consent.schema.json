{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "UserAgreementConsent",
  "type": "object",
  "allOf": [{ "$ref": "../user.consent.schema.json" }],
  "extendsJavaClass" : "io.klustr.schemas.console.consent.UserConsent",
  "properties": {
    "org_id": {
      "type": "string",
      "description": "The organization that the user has accepted."
    },
    "agreement_id": {
      "type": "string",
      "description": "The unique ID of the agreement that was signed off on."
    },
    "version_number": {
      "type": "string",
      "description": "The semantic version number of the agreement.",
      "examples": ["1.0.1", "2.1.2", "3.1.2"]
    },
    "first_seen_date": {
      "description": "The first consent date this experiment was last consented to.",
      "type": "string",
      "format": "date-time"
    },
    "last_seen_date": {
      "description": "The last consent date this experiment was last consented to.",
      "type": "string",
      "format": "date-time"
    },
    "status": {
      "description": "The status of this specific experiment consent.",
      "type": "string",
      "enum": ["active", "revoked", "expired"]
    }
  },
  "additionalProperties": false
}