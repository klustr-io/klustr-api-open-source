{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "UserExperimentConsent",
  "type": "object",
  "allOf": [{ "$ref": "../user.consent.schema.json" }],
  "extendsJavaClass" : "io.klustr.schemas.console.consent.UserConsent",
  "properties": {
    "experiment_id": {
      "type": "string",
      "description": "The ID of the experiment that was consented to."
    },
    "client_id": {
      "type": "string",
      "description": "THe ID of the client that was used when approving the experiment."
    },
    "app_id": {
      "type": "string",
      "description": "THe ID of the application that this experiment was connected to."
    },
    "project_id": {
      "type": "string",
      "description": "THe ID of the project that this experiment was connected to."
    },
    "org_id": {
      "type": "string",
      "description": "THe ID of the organization that this experiment was connected to."
    },
    "first_seen_date": {
      "description": "The first consent date this experiment was last consented to.",
      "type": "string",
      "format": "date-time"
    },
    "last_seen_date": {
      "description": "The last consent date this experiment was last consented to.",
      "type": "string",
      "format": "date-time"
    },
    "status": {
      "description": "The status of this specific experiment consent.",
      "type": "string",
      "enum": ["active", "revoked", "expired"]
    }
  },
  "additionalProperties": false
}